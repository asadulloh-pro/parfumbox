# Optional. When unset in dev, the admin app uses same-origin `/_parfumbox-api`
# (see vite.config.ts proxy) so HTTPS tunnels (ngrok) still reach the API without mixed-content errors.
#
# Only set this if you need a fixed API URL, e.g. production build:
# VITE_API_BASE_URL=http://localhost:3000
