# Optional: backend URL for RTK Query once the API exists.
# VITE_API_URL=https://your-api.example.com
#
# Never put TELEGRAM_BOT_TOKEN here. Vite exposes VITE_* to the browser; the bot
# token is server-only (Nest API `.env` when `apps/api` exists).
