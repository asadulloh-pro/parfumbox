export function CheckoutPage() {
  return (
    <div className="tma-page">
      <h1 className="page-title">Checkout</h1>
      <p className="page-placeholder">
        Phone, name, surname, and birthday will be collected here and submitted
        with the order once the API is connected.
      </p>
    </div>
  );
}
